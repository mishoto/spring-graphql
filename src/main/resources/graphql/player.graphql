type Player {
    id: ID!
    name: String!
    age: Int!
    scores: Int!
    avatar: String!

}

# Root
type Query {
    findAllPlayers: [Player]!
    countPlayers: Int!
}

# Root
type Mutation {
    createAuthor(name: String!, age: Int, scores: Int, avatar: String): Player!
}

